# Method to add numbers
#def add_numbers(one, two):
    #sum = one + two
    #return sum

def add_mult_numbers(one, two, three):
    sum = one + two
    product = sum * three
    return product

print(add_mult_numbers(4,4,6))

#print(add_numbers(6,6))